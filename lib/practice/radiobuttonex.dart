import 'package:flutter/material.dart';
void main(){
  runApp(const MaterialApp(home: Radio_Ex(),));
}
class Radio_Ex extends StatelessWidget {
  const Radio_Ex({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),


    );
  }
}
